<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only additions. The network security configuration referenced here allows plain http, but
  only to the loopback addresses a development server is reached on. It exists in this source set
  alone, so a release build cannot pick it up.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:networkSecurityConfig="@xml/network_security_config" />

</manifest>
