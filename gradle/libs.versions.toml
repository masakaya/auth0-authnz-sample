# Shared by every included build. Each build imports it explicitly from its own settings file.
[versions]
spring-boot = "4.1.1"

[plugins]
spring-boot = { id = "org.springframework.boot", version.ref = "spring-boot" }
